# P2P 游戏游廊开发约定

## 产品目标

这是一个非盈利、以朋友之间一起玩为目标的 P2P 小游戏游廊。重点是：

- 打开页面即可进入游戏；
- 两位玩家通过房间号或随机匹配开始对局；
- 游戏状态尽量通过 WebRTC DataChannel 直连同步；
- 房间、匹配和 WebRTC 信令继续由现有 Worker/D1 负责；
- 多个小游戏共享房间、聊天、Emoji、在线状态和再来一局体验。

## 产品取舍

- 不做账号体系、排行榜、付费、竞技积分和复杂社交关系。
- 不把严格反作弊、外挂检测和不可篡改作为目标；默认“防君子不防小人”。
- 可以采用房主权威模式：创建房间的一方作为 Host，负责验证操作、维护权威状态并广播同步结果。
- 需要保留基本的断线提示、状态序号和重连快照，保证普通网络波动下体验可恢复。

## 不搭中转服务器（重要，不要试图绕过）

只使用公共 STUN 做 NAT 穿透，**不部署 TURN，不部署中继，不部署媒体服务器，不搭自建信令转发**。直连失败就是失败，这是产品接受的正常结果。

- **原因**：这是非盈利的好友小游戏，中转的流量和带宽没有意义；一旦引入中转，还要连带处理临时凭证下发、凭证被白嫖的滥用防护和成本上限。
- **做法**：连不上就如实告诉用户「P2P 连接失败，可能是当前网络限制了直连」，让用户换网络或换个对手。不要加任何兜底转发。
- **不要做的事**：不要为了“提高连通率”引入 TURN / relay / SFU / 自建信令代理；不要写“连不上就退回服务器转发”的逻辑；不要把 TURN 凭证塞进前端或让 Worker 下发；不要把“连通率低”当成待修的 bug。
- **已知代价**：对称 NAT、公司网络、部分移动网络下无法建立连接。这是预期行为。
- 这条约束优先于「保证普通网络波动下体验可恢复」——可恢复指的是同一连接内的重连和状态补发，不是换一条链路。

## 当前开发方向

五子棋继续作为第一款稳定的轮流制游戏。下一款优先尝试双人炸弹人，作为中高难度实时游戏：

- 第一阶段只做 2 人、单张固定地图、移动、放置炸弹、爆炸和胜负；
- 第二阶段再考虑道具、地图变化、3–4 人房间和更多地图；
- 如果实时同步成本过高，可以先实现空气曲棍球或 Pong 作为实时同步验证游戏。

## 通用游戏接口

新增游戏不得重新实现房间和聊天逻辑。游戏模块应尽量提供类似以下能力：

```ts
interface GameDefinition<State, Action> {
  id: string;
  createInitialState(seed?: number): State;
  validateAction(state: State, action: Action, playerId: string): boolean;
  applyAction(state: State, action: Action): State;
  getResult(state: State): "playing" | "draw" | string;
}
```

每个游戏至少需要定义：

- 初始状态和版本号；
- 玩家身份、房主身份和回合/帧状态；
- 客户端输入或操作消息；
- Host 验证规则；
- 权威状态快照和增量同步；
- 结束、重来、退出和断线状态。

## 通信约定

游戏消息统一包含以下字段：

```ts
{
  gameId: string;
  roundId: number;
  seq: number;
  type: string;
  payload: unknown;
}
```

- 轮流制游戏优先发送操作意图，由 Host 校验后广播结果。
- 实时游戏优先发送玩家输入和时间戳，Host 按固定 tick 推进状态。
- 可靠、有序消息用于开始、落子、炸弹、胜负、聊天和重连快照。
- 高频且允许丢失的消息用于悬停、方向输入、光标和临时表现；不要让这类消息阻塞棋局或游戏主状态。
- 每个游戏都必须处理重复消息、乱序消息和旧 `roundId` 消息。
- 不把聊天、Emoji 或临时光效写入权威游戏状态。
- 只有直连一条链路：没有 TURN、没有中继、没有服务器转发（见上一节）。`iceServers` 只放公共 STUN。

## 炸弹人 MVP 约定

- 使用固定地图和确定性地图种子；双方必须能根据同一个 seed 得到相同地图。
- Host 维护玩家位置、炸弹列表、爆炸计时、可破坏方块和胜负状态。
- 客户端发送方向输入和放置炸弹意图，不直接发送最终位置或伤害结果。
- Host 使用固定 tick 更新状态，并定期广播完整快照；客户端只做显示插值，不改变权威结果。
- 第一版优先保证键盘、触屏方向控制、房间加入、断线提示、再来一局和移动端可玩性。
- 避免一开始加入账号、复杂道具、排行榜和大量地图。

## 代码复用和许可证

可以参考和改写公开的非盈利小游戏项目，但“非盈利”不等于可以忽略许可证：

- 优先选择 MIT、BSD、Apache-2.0 等许可清晰的代码；
- 保留原项目版权声明、许可证和必要的 NOTICE；
- 第三方代码、素材、音效和字体分别检查许可证；
- 没有明确许可证的仓库只参考玩法和架构，不直接复制实现或素材；
- 参考项目中的品牌、角色、地图和商业素材不得直接带入本项目；
- 在仓库中维护 `THIRD_PARTY_NOTICES.md`，记录实际复用的代码和素材来源。

## 体验要求

- 游戏大厅优先展示可直接进入的游戏卡片。
- 对局中必须清楚显示玩家身份、连接状态、当前回合/帧状态和退出入口。
- 胜负结果不能遮挡主要游戏区域。
- 对局结束后支持同一房间再来一局，并清理上一局的棋盘、输入和临时状态。
- 聊天、Emoji 和游戏画面保持独立布局；桌面端充分利用空间，手机端不出现横向滚动。
- 新增游戏必须提供等待、连接中、对局中、结束、断线和重连失败状态。

## 验证要求

新增游戏至少验证：

1. 两个浏览器可以通过房间号进入同一局；
2. Host 和 Guest 的关键状态最终一致；
3. 重复、乱序和旧消息不会破坏当前对局；
4. 对局结束后可以在原房间开始下一局；
5. 桌面端和手机端都能完成一局；
6. `pnpm lint`、`pnpm exec tsc --noEmit` 和 `pnpm build` 通过。

